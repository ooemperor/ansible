---
dependencies:
  - role: docker
galaxy_info:
  author: Michael Kaiser
  role_name: docker_monitoring
  description: Setup of docker monitoring tools to enable prometheus to collect metrics
  license: license (MIT)
  min_ansible_version: "2.10"
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all
  galaxy_tags:
    - monitoring
    - prometheus
    - docker
    - deployment
